name: zava_mcp_server

# Note: PostgreSQL is now deployed to Azure
# Local PostgreSQL container has been removed since we use Azure Database for PostgreSQL

services:
  # No services needed - MCP servers run directly via VS Code tasks
  # This docker-compose.yml is kept for potential future local services
